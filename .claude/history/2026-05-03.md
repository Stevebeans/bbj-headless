# 2026-05-03

## Theme of the day
Post-deploy monitoring + Cloudways migration debate + tracking infrastructure setup.

## What got done

### 1. Production deploy (continued from 5/2 work)
- Pushed `staging` → `main` (commit `9d92d12`)
- Deployed WP plugin to production via `bash /c/xampp/htdocs/bbj/.claude/scripts/deploy-plugin.sh bigbrotherjunkies-data --prod` (note: deploy script lives in bbj repo now, not bbj-app — the `/full-push` and `/full-push-live` skills have outdated paths and need updating)
- Both staging + production confirmed deployed successfully

### 2. Post-deploy monitoring observations
- ISR writes spike during first 12-24h is expected post-deploy cache rebuild wave (any layout/lib change invalidates entire app cache)
- Already seeing wins: home page (`/`) at 0 writes, /bigbrother-players writes dropped 833/hr → 228/hr (-73%), edge requests 9.2K/hr → 2.9K/hr (-68%), cache hit rate back to 86%
- /[slug] still inverted (writes > reads) partly inherent to bot wide-and-shallow crawl pattern + still includes layout-level fetches for ad-scripts/ad-settings

### 3. Cloudways migration debate — DECIDED to NOT migrate now
- Explored cloning to a smaller Cloudways server (4GB/2-core/$54 + block storage)
- Best achievable: ~$62/mo vs current $99 = $33/mo savings
- Verdict: not worth the migration risk + time during BB28 prep period
- Better strategic order: image migration to R2 → BB28 peak data → off-season decision with real numbers
- End-state target: small DO droplet ($12) + R2 ($1) + Cloudflare = ~$15-20/mo (saves ~$80/mo vs current)
- User OK going self-managed with my help, but only AFTER BB28 wraps

### 4. Tracking infrastructure created
- **`.claude/data/cloudways/`** — monthly Cloudways server stats log
  - README with filing convention, stats log table, decision triggers
  - User will drop screenshots as `YYYY-MM.png`
- **`.claude/data/scores/`** — Lighthouse score tracking
  - README with testing methodology (incognito, mobile, DevTools Lighthouse)
  - `scores.md` baseline row populated for 2026-05-03 with BBJ vs BBN data
  - Both BBJ (Perf 34) and BBN (Perf 54) on Freestar — direct apples-to-apples competitor benchmarking

### 5. Monthly cron routine created
- Routine `trig_01HicLtxHtotiB7TBkt5kY3p` — fires 1st of every month at 9am ET
- Pushes notification reminding user to screenshot Cloudways stats
- https://claude.ai/code/routines/trig_01HicLtxHtotiB7TBkt5kY3p
- Next fire: 2026-06-01 13:06 UTC

### 6. Tooling discoveries
- DevTools Lighthouse > PSI for ad-heavy sites that timeout PSI with `RPC::DEADLINE_EXCEEDED`
- Treemap revealed BBJ is 80%+ ads by JS bundle weight (Primis video alone = 762 KiB / 29% of total bundle)
- Next.js app code is ~200 KiB total — small and well-built
- All performance issues are downstream of ad infrastructure, not app code

### 7. Defensive fixes
- `getRelatedPosts` 400 error from WP — added parseInt + Number.isFinite guards (already shipped in 5/3 deploy)

## Punch list for NEXT WEEKEND (5/9-10 ish)

User's plan: monitor cache + Vercel data through the work week, then dig in next weekend.

### Priority order

1. **Granularize the Purge Cache button** ← user-requested 5/3 evening
   - Current: `revalidatePath('/', 'layout')` — nuclear, equivalent to a deploy in cache impact
   - Should be: targeted purge options. Choose:
     - Spoiler bar / players / seasons (specific data tag)
     - Posts / blog content
     - Feed updates
     - Home page only
     - Full nuke (keep as a separate "danger" button)
   - UI: dropdown or multi-select on the admin overview, not a single button
   - Why: nuclear purge causes the same write-spike wave as a deploy. Defeats the whole point of webhook-driven granular invalidation.

2. **Home page redesign** (user's primary plan)
   - Mostly design work, few hours
   - Bring in any new data display patterns at this point
   - Remember caching-first rule — every new fetch declares cache strategy

3. **PSI/Lighthouse tuning** (after redesign settles)
   - Hero LCP fix: `priority` + explicit width/height (BBN beats us 2.7s vs 4.9s on this)
   - CLS fix: investigate which ad slot heights aren't reserved (BBN beats us 0.011 vs 0.185)
   - Font diet: 4 Google Fonts → 2

4. **`/compare/[matchup]` crawl trap mitigation** (annoying but cheap, not urgent)
   - Bots ignoring robots.txt + noindex (chronic since April)
   - 915 writes/hr / 465 unique paths in last hour observed 5/3
   - Cost is minimal due to Fluid Compute, just messy in metrics
   - Easiest fix: Cloudflare WAF rule blocking `/compare/*` from `cf.client.bot = true` (~5 min)
   - Bundle with the rest of next weekend's deploy to avoid extra rebuild wave
   - User does NOT want to block all bots — only crawler bots specifically

5. **Profile + season page redesigns** (user mentioned in passing)
   - Same "mostly design" effort as home page
   - Down the line, not necessarily next weekend

### Off-season (Sept-Oct after BB28)

- Image migration to Cloudflare R2
- Eventual DO droplet self-managed migration (after R2 is solid)
- Eventual Cloudways downgrade or full provider switch
- Off-season is also when better real-load data exists for sizing decisions

## Reminders

- **Don't deploy this week.** Each deploy = another cache rebuild wave. Goal is clean post-deploy steady-state data over Mon-Fri.
- **Friday Cloudways screenshot ritual** — save to `.claude/data/cloudways/2026-05.png`
- **DevTools Lighthouse** is the workaround for PSI timeouts — same engine, runs locally
- **`/full-push` and `/full-push-live` skill paths are outdated** — point at `/c/xampp/htdocs/bbj-app/.claude/scripts/` but actual script lives at `/c/xampp/htdocs/bbj/.claude/scripts/`. Worth fixing the skill definitions sometime so they don't error on first try.
- Default deploy = staging unless user explicitly says "live" / "production"
