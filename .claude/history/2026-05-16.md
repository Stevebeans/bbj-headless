# 2026-05-16 — Homepage v2 redesign + paginated archive

## Shipped (commit 3c09d53, pushed to origin/staging)

Full session focused on porting the v2 PHP theme look into bbj-app.

### Visual / layout
- **Paper bg site-wide** — added `paper: #F0E8D5` + `paper-2: #E8E0CC` to `tailwind.config.js`. Swapped `bg-slate-200` → `bg-paper` in `globals.css`, `src/app/layout.jsx` body class, and 3 spots in `src/app/admin/layout.jsx`.
- **Hero rewrite** (`src/components/home/Hero.jsx`) — matches v2 `hero-post.php` 2-col card (image left, content right). Byline pushed to bottom via `mt-auto`. Uses new `<SectionHeader as="h1">`.
- **Feed Updates section** — full v2 port:
  - `FeedUpdatesSection.jsx`: white card + linked `<SectionHeader>` "Latest from the Feeds". Splits cards into 4 / 8 / 3 with inline ads after card 4 and card 12. User manually removed the `border-l-2` rail from the column wrappers.
  - `FeedUpdateCard.jsx`: time rail (left, hidden <sm) | dot | white card. Title in Yanone (`font-display`), excerpt line-clamp-3, centered image, recent-replies box, footer `@author-slug · replies · Join the thread →` with Quick Reply button on the right. Kept the interactive quick-reply form + recent-comments preview after the user asked for them back. Avatar dropped intentionally.
- **More Stories** (`MoreStories.jsx`) — wrapped in container, renamed "More Big Brother Stories & News", uses `<SectionHeader>`, ad after card 5, footer link "See more stories & news →" to `/page/2`.

### New components
- `src/components/home/SectionHeader.jsx` — reusable section header. Defaults `h2`, accepts `as="h1"`, optional `href` wraps in Link. **Every future section header on bbj-app should use this** — user explicit ask.
- `src/components/home/StoryCard.jsx` — extracted from MoreStories so the archive route can reuse it.
- `src/components/ui/Pagination.jsx` — numbered pagination with Prev/Next, ellipses, current ± 1. Page 1 always links to `/` (not `/page/1`).

### Paginated archive
- New route `src/app/page/[page]/page.jsx` — 20 posts/page, ISR (`revalidate: false`, tag `posts`), redirects `/page/1` → `/`, 404 on invalid/out-of-range. Same sidebar as `/live-feed-updates`. Ads after card 4 and card 12.
- New API client fn `getPaginatedPosts({ page, perPage })` in `src/lib/api/posts.js`.

### Ad placements
- All inline ads use `bigbrotherjunkies_middle_feed` placement with `showBranding={false}` for inline feel, distinct `slotId`s per insertion: `feed_inline_1`, `feed_inline_2`, `more_stories_inline`, `archive_inline_1`, `archive_inline_2`.
- Homepage now has 5 ads using the same placement name (relies on Freestar's slotId disambiguation). If any fail to fill, register named placements in Freestar dashboard.

## Plugin changes (deployed to STAGING Cloudways)
Local edits at `C:\xampp\htdocs\bbj\wp-content\plugins\bigbrotherjunkies-data\`:
- `src/Api/FeedUpdateRoutes.php:699` — thumbnail size `medium` → `medium_large` (768px). Legacy posts with small originals need a thumbnail regen to backfill.
- `src/Api/HomeRoutes.php` `/posts` endpoint — `no_found_rows` flipped to `false`, response now includes `total_pages`, `total_posts`, `current_page`. Additive change.

Both deployed via manual tar+scp pipeline (deploy script `.claude/scripts/deploy-plugin.sh` is missing from this repo — used the documented commands instead). User authorized "we can keep doing that" for staging plugin deploys.

**Not deployed to prod** — user's holding all prod deploys.

## Known caveats / follow-ups
1. **Page 2 starts at post 21 not post 11** — homepage shows 10, archive shows 20/page starting from post 1. Posts 11–20 don't appear on `/page/2`. Fix needs `offset` param in `/posts` API. User hasn't flagged it yet.
2. **5 ads with same `middle_feed` placement on homepage** — may need to register dedicated placements in Freestar if duplicates don't fill.
3. **Feed update images for legacy posts** — `medium_large` may not exist for very old uploads; WP falls back to original. Thumbnail regen needed eventually.
4. **WIP files NOT included in commit** (still uncommitted locally):
   - `package.json`, `src/app/page.jsx`, `src/components/ads/FreestarSDKLoader.jsx`, `src/components/layout/Header.jsx`, `src/components/layout/MobileMenu.jsx`, `src/components/spoiler-bar/SpoilerBar.jsx`, `src/components/spoiler-bar/SpoilerBarWrapper.jsx`
   - Untracked: `src/app/api/spoiler-bar/`, `src/hooks/useBbTime.js`, `.claude/projects/stress-test/`, `.claude/update-history/staging-update-2026-05-09.md`, `.claude/history/2026-05-15.md`
   - These are pre-existing WIP from prior sessions — left untouched.

## Build status
Pushed `cc77e01..3c09d53` to `origin/staging` → **Vercel staging build triggered**. User had earlier expressed wanting to avoid Vercel rebuilds this weekend but then explicitly authorized the push; flagging in case the build is unwanted.

## Env note
`.env.local` points dev server at staging WP (`stg-wp.bigbrotherjunkies.com`). Local plugin edits at `bbj.localhost` are invisible to the dev server unless `.env.local` is flipped + dev restarted. Must deploy plugin to staging to see API changes.

---

## Evening session — post page top area (NOT yet committed)

User started on roadmap item #1 (finalize post page). Did the v2 single-post top area only — body content untouched per explicit ask.

### Changes (local, uncommitted)
- **NEW** `src/components/posts/PostHeader.jsx` — kicker (red dot + categories, font-osw uppercase, text-accent-red) + Yanone h1 (text-3xl md:text-4xl, font-bold, gray-900, leading-[1.1]) + byline strip (avatar 44px + author name in osw + Published `Mon D, YYYY · h:mm A PT` + X min read + comments link anchored to #comments) + ShareButtons on the right. Read time = `Math.max(1, round(words / 200))` computed from stripped content.
- **NEW** `src/components/posts/ShareButtons.jsx` — client component (needs `navigator.clipboard`). X / FB / Reddit intent URLs + Copy Link with green checkmark on success. Uses new `.bbj-share-btn` class.
- **MODIFIED** `src/components/posts/PostHero.jsx` — stripped the comment-count overlay badge. Now just a clean `<figure>` with rounded image, `priority` + `fetchPriority="high"`.
- **MODIFIED** `src/app/[slug]/page.jsx` — added `p-5 md:p-[22px]` to article wrapper, replaced inline `<h1>` + `<PostMeta>` with `<PostHeader>` for posts (pages keep old simpler layout: blue h1 + clean PostHero). Image now renders AFTER byline (matches v2 order). Body wrapper `mt-6` for spacing under image.
- **MODIFIED** `src/styles/globals.css` — added `.bbj-share-btn` and `.bbj-share-btn.is-copied` utility classes.
- **ORPHANED** `src/components/posts/PostMeta.jsx` — no longer imported anywhere. Left in place for now in case bits get repurposed.

### Tweaks user made during session
- Removed the excerpt block below h1 (was rendering `content.excerpt` styled like a v2 subtitle)
- Asked for smaller title — bumped down from `text-4xl md:text-5xl lg:text-6xl` → `text-3xl md:text-4xl`

### Next session focus
User said: "I'll be working on the feed update thing inside blog posts" — meaning `src/components/posts/FeedUpdates.jsx` (the singular in-post feed updates component, NOT the homepage `FeedUpdatesSection`). See `.claude/projects/pre-prod-checklist.md` for the suggestions list (reuse homepage `FeedUpdateCard`?, SectionHeader for the date header, match ad interval pattern).

### Not committed → no Vercel build triggered
Holding the commit per user's no-Vercel-build-this-weekend preference. When user is ready to push, the working tree from this session adds: PostHeader.jsx, ShareButtons.jsx, modifies PostHero.jsx + [slug]/page.jsx + globals.css.
