# 2026-02-05 — Lighthouse Fixes & Ad Script Integration

## What Was Done

### 1. Lighthouse Accessibility Fixes
**Commit:** `2465a7f`

- **ARIA fix** — Added `role="menuitem"` to desktop nav links/buttons in Header.jsx (menubar required menuitem children)
- **Contrast fixes** — Bumped `text-gray-400` → `text-gray-500` in multiple components (Hero time_ago, FeedUpdateCard Quick Reply, character count, evicted player stats)
- **Houseboard** — Power of Veto badge `bg-yellow-500` → `bg-yellow-600` for better white text contrast
- **Touch targets** — Added `py-0.5` to player name links in stats leaderboard
- **Ad placeholder** — Slot label `text-gray-300` → `text-gray-400`

Result: Score went from previous to **97**.

### 2. Ad Manager Script Splitting
**Commits:** `7352af2`, `48e7160`

Split the ad manager's "Header & Footer Code" into two categories:
- **Global Scripts (All Users)** — Analytics, tracking pixels, always loaded (e.g., GA4)
- **Ad Network Scripts (Hidden for Ad-Free)** — AdSense, Freestar, completely blocked for supporter roles

**Backend (SettingsPage.php):**
- New fields: `global_header_code`, `global_footer_code`, `ad_header_code`, `ad_footer_code`
- Migration: existing `header_code`/`footer_code` auto-populate into ad fields on first save
- Updated admin UI with two clearly separated sections

**API (AdRoutes.php):**
- `/bbjd/v1/ad-scripts` now returns `global_header`, `global_footer`, `ad_header`, `ad_footer` plus combined `header`/`footer`
- Falls back to old field names for backward compatibility

**Frontend (layout.jsx):**
- Server-side rendering of all scripts in `<head>` and before `</body>`
- Uses `getInitialAuthState()` to check supporter roles server-side
- Global scripts always render; ad scripts only render for non-supporters
- Helper functions `extractInlineScript()` and `extractExternalScripts()` parse HTML script tags into React elements

**Plugin deployed to production via SSH.**

### 3. Google Analytics Integrated
- GA4 tag `G-1Q771W4ZV2` added to Global Scripts header in WP admin
- Verified loading on staging.bigbrotherjunkies.com

## Files Modified
- `src/components/layout/Header.jsx` — ARIA menuitem roles
- `src/components/home/Hero.jsx` — Contrast fix
- `src/components/home/FeedUpdateCard.jsx` — Contrast fixes
- `src/components/home/HomeWidgets.jsx` — Contrast, touch targets, PoV badge color
- `src/components/ads/AdBlockerMessage.jsx` — Contrast fix
- `src/app/layout.jsx` — Ad script injection server-side
- `src/lib/api/ads.js` — Added `getAdScripts()` function
- `wp-plugin/.../Admin/Pages/SettingsPage.php` — Split script sections
- `wp-plugin/.../Api/AdRoutes.php` — Updated ad-scripts endpoint

## Status
- Ad system plumbing complete, pinned for now — real ad testing when ready for live
- GA4 tracking active on staging

---

# 2026-02-05 (Session 2) — Billing Polish: Payment Method, History, Plan Changes

## What Was Done

Implemented three new billing features in the Premium tab of Settings:

### Feature 1: Payment Method Display
- **StripeService.php**: Added `getPaymentMethod()` — fetches default payment method from Stripe API
- **BillingRoutes.php**: Enriched `/billing/subscription` with `payment_method` field
- **settings/page.jsx**: Shows "Visa ending in 4242" + "Update payment method" link

### Feature 2: Payment History
- **BillingRoutes.php**: Added `GET /billing/invoices` endpoint
- **billing.js**: Added `getInvoices()` function
- **settings/page.jsx**: Payment History table with skeleton loading

### Feature 3: Upgrade/Downgrade Plans
- **ApiSettingsPage.php**: Added `stripe_price_monthly/annual/lifetime` settings
- **StripeService.php**: Added `getStripePriceId()` and `updateSubscription()` (price swap with proration)
- **SubscriptionManager.php**: Added `changePlan()` with validation
- **BillingRoutes.php**: Added `POST /billing/change-plan` route
- **billing.js**: Added `changePlan()` function
- **settings/page.jsx**: Change Plan section + confirmation modal + PayPal error handling

### Code Cleanup
- Fixed duplicate PayPalService instantiation
- Extracted `refreshSubscription()` helper
- Extracted `getInvoiceStatusClasses()` to replace nested ternary
- Stored Stripe Price IDs in constructor

## Files Modified
- `bigbrotherjunkies-data/src/Billing/StripeService.php`
- `bigbrotherjunkies-data/src/Billing/SubscriptionManager.php`
- `bigbrotherjunkies-data/src/Api/BillingRoutes.php`
- `bigbrotherjunkies-data/src/Admin/Pages/ApiSettingsPage.php`
- `bbj-app/src/lib/api/billing.js`
- `bbj-app/src/app/settings/page.jsx`

## Deployment & Configuration

### Plugin deployed to production via SSH

### Stripe (DONE)
- Live keys configured (publishable, secret, webhook secret)
- Webhook created: `https://bigbrotherjunkies.com/wp-json/bbjd/v1/billing/webhook/stripe`
- Stripe Price IDs entered by user:
  - Monthly: `plan_QW7R1t9l8QEQ0A`
  - Annual: `price_1Pbsj5Gjb8i2IhlbGptLU92S`
  - Lifetime: `price_1SxgNbGjb8i2Ihlby8sMsSWz`
- Test mode turned OFF

### PayPal (DONE — needs cache clear)
- Live keys configured (client ID, secret, webhook ID `4JB42014CP885693B`)
- Webhook already existed: `https://bigbrotherjunkies.com/wp-json/bbjd/v1/billing/webhook/paypal`
- Created subscription plans via REST API:
  - Monthly: `P-4CP04044DV7153737NGCW6RY` ($6.95/mo)
  - Annual: `P-46C17427LU1157813NGCW6VI` ($35/yr)
- Product ID: `PROD-8BX72953YY772871N`
- Sandbox mode turned OFF

### BLOCKER: Redis Object Cache
- PayPal Plan IDs are in the database but **NOT showing in WP Admin** due to Redis serving stale cache
- **Fix on next session:** Open WP Admin > API Settings, paste the two PayPal Plan IDs into the empty fields, and hit Save. This will update both DB and Redis cache in one shot:
  - Monthly Plan ID: `P-4CP04044DV7153737NGCW6RY`
  - Annual Plan ID: `P-46C17427LU1157813NGCW6VI`
- Alternatively: flush Redis via Cloudways panel (Server > Redis > Purge)

## Status
- All PHP files pass syntax checks
- Plugin deployed to production
- **NOT committed to git** — Next.js frontend changes still local
- Next steps: fix Redis cache issue, verify settings page shows all values, test a live subscription flow, then commit
