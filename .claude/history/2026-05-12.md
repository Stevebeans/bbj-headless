# 2026-05-12 — Session Checkpoint

Long session spanning 2026-05-09 → 2026-05-12. User is about to `/clear`. This is the catch-up note.

## ⭐ TOP PRIORITY AFTER CLEAR

**User wants a scan of the v2 PHP theme to assess how far along it is** — `C:\xampp\htdocs\bbj\wp-content\themes\bbj-v2-theme`. Purpose: apples-to-apples comparison with the headless bbj-app (which I estimated ~84% done on started work). The user is torn between staying on the headless Vercel setup vs. reverting to the PHP v2 theme, and keeps getting nudged toward PHP by claude.ai-the-chat (which only sees what the user pastes). They want *me* — same engine that knows bbj-app + the full project context — to directly read the v2 theme and give an honest completeness estimate. Do this fresh after the clear.

## What happened this session

### 1. v2 homepage match (bbj-app) — BUILT, on branch, not merged
- Brainstormed → spec → plan → executed via subagents. Branch `feature/v2-homepage-match` in both repos (bbj-app ~24 commits ahead of staging; bigbrotherjunkies-data plugin ~5 commits ahead).
- New components: `StatusStrip` (page H1 + scrolling ticker), `HouseStrip` (horizontal HOH/Veto/Noms/Have-Nots, moved out of sidebar), `HousePulse` (8h feed-update bar chart, active-season gated), `ParamountPlusCard`, `StickyAdSlot`. Hero `h1→h2` ("Latest Update"). SeasonStats restyled (data unchanged). `Sidebar.jsx` restructured (Paramount+ at top, sticky ad at bottom; SubscribeWidget moved to per-page children → 16 non-home pages updated).
- WP plugin: `getHousePulse()` + `getCurrentSeason()` added to `/bbjd/v1/homepage` payload (`HomeRoutes.php`). `wp_date()` for timezone-correct labels.
- Build clean (46 pages). Spec: `docs/superpowers/specs/2026-05-09-v2-homepage-match-design.md`. Plan: `docs/superpowers/plans/2026-05-09-v2-homepage-match.md`.
- **Open loose end:** `live-feed-updates/page.jsx` still has legacy Houseboard/SocialFollow/WatchLiveFeeds widgets → duplicate Paramount+ link there. Not merged/deployed yet.

### 2. Ad Preview Mode (bbj-app) — BUILT, on same branch
- Admin-only toggle on `/admin/ads` that swaps all `<FreestarSlot>` for sized placeholder cards (size + label). Cookie `bbj_ad_preview`, gated by admin/editor role in `AdContext`. Spec + plan in `docs/superpowers/`. New `AdPlaceholder.jsx`, `width`+`label` added to `src/config/ads.js`.

### 3. 🚨 Production outage — Cloudflare Error 1014 (the big one)
- `bigbrotherjunkies.com` went down with **1014 "CNAME Cross-User Banned"**. Root cause: apex/www were `CNAME → secure.cloudways.cloud` — leftover from a *defunct* Cloudways "Cloudflare Enterprise" add-on. Cloudways purged the orphaned custom-hostname registration → 1014. Nobody changed DNS; Cloudways changed their side. Also revealed `wp.bigbrotherjunkies.com` (the headless WP backend) had **no DNS record at all**, and that the live site had NOT actually been on Vercel — the April flip had been rolled back; prod was on Cloudways/WordPress this whole time.
- **Fixed (all in Cloudflare DNS):** created `A wp → 104.236.65.203` (DNS-only), repointed apex `CNAME @ → 60314f5eb5af1035.vercel-dns-017.com` (Proxied), `www` same target (Proxied), deleted stale `_cf-custom-hostname` TXT. Cloudways primary domain left as `wp.bigbrotherjunkies.com`.
- **Verified working:** apex → 200 with `x-vercel-cache: HIT` behind CF-RAY (Vercel + CF proxy, as intended). `wp.` → 200 nginx. www → 301 to apex. Homepage serves real Next.js HTML.
- Full writeup + correct DNS layout table + hard rules: `memory/project_cloudflare_1014_incident.md`. **This was effectively the first real production cutover to Vercel** — serving the `main` branch (pre-v2-homepage).
- **Pending:** redeploy `main` on Vercel to flush any stale 404/empty ISR entries from the window when `wp.` didn't resolve. Then spot-check homepage / a post / a player page. `staging.bigbrotherjunkies.com` is also broken in Vercel (separate, low priority).

### 4. Cost / "should I revert to PHP" discussion (unresolved, but framed)
- Weekend Vercel cost was ~$0.20-0.30/day — but partly an artifact (apex was 1014ing, traffic wasn't reaching Vercel). `memory/project_vercel_post_cleanup_baseline.md` updated. Real baseline needs re-measuring now that the apex is genuinely on Vercel.
- User is anxious about an unbounded Vercel bill. Framework given: the cost *bombs* (April `force-dynamic`, May layout-fetch cascade) are fixed; what's left (broad ISR revalidation tags) is bounded/optimizable. **Recommended: set a Vercel Spend Management limit to ~$200 with action = Notify (NOT Pause — Pause = self-inflicted outage) and alerts at 25/50/75/100%.** The notification is the circuit breaker. PHP is not actually safer at BB28 peak (2-3k concurrent during live shows is exactly where a single Cloudways server struggles and Vercel+CF caching shines). PHP fallback is cheap though — `bbj-v2-theme` already exists, "revert" = flip DNS back + ensure theme is current, ~1-2 days not a rewrite.
- User's stated plan: cap $200, Notify. If 50% alert mid-month → let it ride / raise. If 50% alert by day ~7 → investigate which categories ($15/day burn), tweak over 2-3 days; if fixable, ride; if not, take days off and switch to PHP. Decision point that matters = first BB28 episode (July).

### 5. Bot protection removal — STILL PENDING
- User intended to remove the Cloudflare AI-bot blocks today (currently 403'd: PerplexityBot, OAI-SearchBot, ChatGPT-User, Perplexity-User — keep GPTBot/ClaudeBot/anthropic-ai blocked). Deferred because of the server issue, which also reset their "observe the cost baseline for a few days" clock — they feel back at day 0. No changes made.

## State of branches
- `bbj-app` `feature/v2-homepage-match` — ~24 commits ahead of staging (v2 homepage + ad preview mode). NOT merged, NOT deployed. Production is on `main`.
- `bigbrotherjunkies-data` plugin `feature/v2-homepage-match` — ~5 commits ahead of staging.
- Live prod = `main` branch on Vercel (pre-v2-homepage), as of today's cutover.

## Headless bbj-app completeness estimate (for the v2 comparison)
~84% done on started work. Content pages ~95% (home on branch), auth ~90%, comments ~95%, admin ~90%, search ~90%, newsletter ~85%, ads ~85%, premium/supporter ~50%, PWA ~30%, **prod cutover to Vercel ~95% (done today)**. Remaining: merge v2 homepage, watch the freshly-live site 2-3 days, decide premium/PWA scope, fix staging domain, redeploy main.
