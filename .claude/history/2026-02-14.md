# Session: Feb 14, 2026

## What Was Done

### User Cleanup Tool (WP Admin)
- Built `UserCleanupPage.php` — new WP admin page under BBJ Data > User Cleanup
- Three batch actions: delete July 2024 spam supporters (7,248), delete roleless users (~16,568), demote remaining supporters to subscriber
- Safety: never touches lifetime, admin, helper, updater, or users with posts/comments
- Registered in AdminLoader, added slug to isPluginPage, rebuilt Tailwind CSS
- Committed `f0822a9`, deployed to staging

### DB Investigation Findings
- 55,917 total users, only 1,203 active (posted or commented)
- 7,248 spam supporters all created July 2024 (gibberish usernames, random @Gmail.com)
- ~16,568 users with no wp_capabilities (2015-2020 Russian spam bots)
- 4 lifetime users — user confirmed all 4 stay as-is (2 from Stripe, 2 probably PayPal)

### PageSpeed Optimization
- Performance: 85 → **96** (mobile)
- SEO: 92 → **100**
- LCP: 4.4s → **2.6s**
- Changes:
  - Added `fetchPriority="high"` to hero image in Hero.jsx
  - Moved all ad/analytics scripts from blocking `<head>` to deferred `<Script>` (afterInteractive/lazyOnload)
  - Fixed "Read More" non-descriptive link text (added aria-label + sr-only title)
  - Added robots meta with googleBot max-image-preview directives
  - Added security headers in next.config.js (HSTS, X-Frame-Options, CSP, etc.)
- Committed `8f772ef`, pushed to staging

### Pre-Launch Checklist Added to Roadmap
- New section covering: SEO (sitemap, robots, redirects, canonical), security headers, environment/infrastructure, testing, post-cutover monitoring
- Updated timeline to include Pre-Launch and DNS Switch as June 2026 phases

## Commits
- `f0822a9` — Add WP admin user cleanup tool for spam purge and supporter demotion
- `8f772ef` — Improve PageSpeed scores: defer scripts, fix LCP, add security headers

## User's Bug List (Not Yet Fixed)
1. Bug report system not sending bugs
2. Home page comment counts need to link to comment area
3. Fine-tune permissions — role-based permission selector (like WordPress)

## Next Session
- User is entering polish/testing mode — going between live and staging
- User cleanup tool is ready to use on staging/production
- The 3 bugs above are queued for when user is ready
