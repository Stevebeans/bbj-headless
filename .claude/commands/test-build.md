# Test Production Build Locally

Build and serve the production version locally to test before pushing.

## Instructions

1. Run `npm run build` to create the production build
2. If the build succeeds, run `npm run start` to serve it locally
3. Let the user know the production build is running at http://localhost:3000
4. If the build fails, show the errors so they can be fixed
