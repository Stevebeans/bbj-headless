# Start Development Server

Run the Next.js development server with hot reload.

## Instructions

Run `npm run dev` in the project directory.

Let the user know the dev server is starting at http://localhost:3000
